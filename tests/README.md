# System-level and hardware test procedures

Step-by-step procedures for `HW-*` tests in `TEST_PLAN.md` (board setup, bitstream tag, COM port,
MATLAB command, pass criterion) and integration/validation tests of the full system.
Results go to `docs/verification/` using `_TEMPLATE_test_report.md`.
