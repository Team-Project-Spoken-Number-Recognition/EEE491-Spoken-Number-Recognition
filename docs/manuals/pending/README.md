# PENDING — not released

These manuals were received from course staff but are **not yet released** to the team workflow
(staged-manual rule, see `CLAUDE.md` and `docs/manuals/README.md`).

- Do **not** implement anything from these files.
- AI sessions must **not** read them unless a team member explicitly releases one.
- To release: move the PDF to `docs/manuals/`, update the register, write the NEW MANUAL ANALYSIS in
  `docs/manual_analysis/`, then update REQUIREMENTS / ARCHITECTURE / PROJECT_STATUS / HANDOFF.
