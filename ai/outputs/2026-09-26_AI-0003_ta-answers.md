# AI-0003 — TA answers integrated; reference design consulted

```text
ID:            AI-0003
Date:          2026-09-26
Team Member:   eeerenbuyukbas
AI Tool:       Claude Code, desktop app (Code tab)
Model:         Claude Opus 5.5 (claude-opus-5-5)
Purpose:       Record the lab TA's answers to Q-02…Q-13 and propagate them through the project documents;
               apply the team rule "where choices are left to us, follow the reference design".
```

## Prompts (summary — verbatim in the session export)
1. "Hande starts DEBUG today — should I start CTRL or wait?" → advice: start in parallel (handshake is
   manual-defined), agree handshake details first.
2. TA answers to Q-02…Q-13 given by the team, plus two team instructions: follow Borek-32's specification
   where the TA leaves the choice to us (Q-05), and choose the baud rate as in that project (Q-06).

## Input Data
- TA answers (Turkish, relayed by eeerenbuyukbas).
- Reference design `Borek-32/EEE491-Spoken-Digit-Recognizer-BASYS3` (public, MIT): only
  `constraints/jc_homefab.xdc`, `rtl/debug.vhd` (header/generics), `rtl/top_module.vhd` (ports, debug
  generic map) and the file list were read. No future-stage code was read.

## AI Output
- INSTRUCTOR_QUESTIONS answers; new Q-14 (to Enis Hoca: DCT/COMPARE → NN labs?).
- REQUIREMENTS: REQ-SYS-006 (digits 0–9), REQ-IF-006/007 (ready after reset, start while busy),
  REQ-DEBUG-004/007/009/013/014/016, REQ-CTRL-006, REQ-PERF-003, REQ-SYS-002 note; owners set to names.
- ASSUMPTIONS: -006/-007/-008 VERIFIED; -003 supported by reference design; unknowns U-03/06/07 resolved,
  U-04 conflict, U-16/17/18/19/21 updated.
- DECISIONS: DEC-003 (demo no longer depends on CTRL), DEC-005 (baud conflict), DEC-007 (RESET = BTNC),
  DEC-012 (parameter named `N`), new DEC-018 (reference-design rule).
- INTERFACES board pin table; ARCHITECTURE demo diagram without CTRL; TEST_PLAN; PROJECT_TIMELINE §4;
  TEAM_MANUAL §6.5; CLAUDE.md (source-of-truth order + reference design section); PROJECT_STATUS
  (R-02 closed, R-15, R-16); DEBUG manual analysis; HANDOFF.

## Items flagged for the team (AI judgement, not facts)
- **Baud rate:** AI recommended 115 200 because of the TA's "No" to Q-06. **Team decision (eeerenbuyukbas):
  1 000 000 baud** — the TA misunderstood the question as changing the rate between labs; the instructor leaves
  the rate to the team. AI recommendation not adopted; DEC-005 updated.
- The reference `debug.vhd` is the final multi-RAM debugger, not the Lab-DEBUG 2^N module → reference only.
- Parameter to be named literally `N` (TA wording) instead of `G_ADDR_WIDTH`.
- Pin mapping from the reference XDC still to be cross-checked with Digilent's official XDC.

## Self-detected error
While re-counting requirements the AI found that its AI-0001 report stated 64 requirements instead of 61
→ corrected, logged as `ai/corrections/2026-09-26_CORR-0001_requirement-count.md`.

## Human Review / Detected Issues / Correction / Final Decision
_PENDING_

## Verification
Documentation only. TA answers are relayed by a team member (not verbatim from the TA).

## Related Commit
Branch `docs/ta-answers-2026-09-26` (not yet committed).
